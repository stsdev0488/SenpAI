import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  componentSpace: {
    marginLeft: 5,
  },
  image: {
    width: 20,
    height: 20,
    resizeMode: 'contain',
  },
});
