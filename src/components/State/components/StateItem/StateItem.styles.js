import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 12,
    lineHeight: 14,
    color: '#FFFFFF',
    marginBottom: 5,
  },
  componentSpace: {
    marginLeft: 5,
  },
  itemsContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
