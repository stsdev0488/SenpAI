import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderWidth: 2,
    borderColor: '#000000',
  },
  imageWrapper: {
    width: 24,
    height: 24,
    overflow: 'hidden',
  },
  image: {
    width: 240,
    height: 96,
  },
});
