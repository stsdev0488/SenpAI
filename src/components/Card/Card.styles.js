import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    borderRadius: 12,
    shadowOpacity: 0.03,
    shadowOffset: {
      width: 0,
      height: 10,
    },
    shadowColor: '#000000',
    shadowRadius: 40,
    overflow: 'hidden',
  },
});
