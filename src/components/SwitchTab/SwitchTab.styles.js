import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    paddingHorizontal: 15,
    paddingTop: 8,
    paddingBottom: 16,
  },
  tabText: {
    fontSize: 13,
    lineHeight: 15,
    fontWeight: 'bold',
  },
});
